# iOS 앱 설정 완료! 🎉

## ✅ 완료된 작업

1. **Capacitor 설치 및 초기화** ✓
   - `@capacitor/core`, `@capacitor/cli` 설치
   - 앱 ID: `com.aptgugu.calculator`
   - 앱 이름: `아파트 대출 계산기`

2. **iOS 플랫폼 추가** ✓
   - `@capacitor/ios` 설치
   - iOS 네이티브 프로젝트 생성
   - 위치: `/ios/App/`

3. **Next.js 정적 빌드 설정** ✓
   - `output: 'export'` 설정
   - `trailingSlash: true` 설정
   - `images.unoptimized: true` 설정
   - 동적 라우트 및 API 제거 (정적 빌드 호환성)

4. **구글 광고 제거** ✓
   - 모든 AdSense 코드 제거
   - iOS 앱에서는 광고 표시 안 됨

5. **Capacitor 설정** ✓
   - Safe Area 자동 처리
   - Splash Screen 플러그인 설치 및 설정

6. **빌드 및 동기화** ✓
   - Next.js 정적 빌드 성공 (21개 페이지)
   - iOS 프로젝트에 웹 파일 동기화 완료

7. **Xcode 프로젝트 열기** ✓
   - Xcode가 자동으로 열림

---

## 📱 다음 단계: Xcode에서 해야 할 일

### 1. 프로젝트 설정 확인

Xcode가 열리면 다음 설정을 확인하세요:

#### General 탭
- **Display Name**: 아파트 대출 계산기
- **Bundle Identifier**: com.aptgugu.calculator
- **Version**: 1.0.0
- **Build**: 1
- **Deployment Target**: iOS 13.0 이상 권장

#### Signing & Capabilities 탭
⚠️ **중요**: Apple Developer 계정이 필요합니다 ($99/년)

1. **Team** 드롭다운 클릭
2. **Add an Account...** 선택 (아직 로그인 안 한 경우)
3. Apple ID로 로그인
4. **Automatically manage signing** 체크
5. Team 선택 (본인의 Apple Developer 계정)

---

### 2. 앱 아이콘 설정

**필수 작업**: 앱 아이콘을 추가해야 합니다.

#### 아이콘 준비
- **크기**: 1024x1024px
- **형식**: PNG (투명 배경 불가)
- **파일명**: AppIcon.png

#### 아이콘 추가 방법
1. Xcode 좌측 네비게이터에서 `App` 선택
2. `Assets.xcassets` 클릭
3. `AppIcon` 클릭
4. 1024x1024 슬롯에 아이콘 이미지 드래그 앤 드롭

**임시 아이콘이 필요한 경우**:
- https://www.appicon.co/ 에서 무료 아이콘 생성 가능

---

### 3. 시뮬레이터에서 테스트

#### 방법 1: Xcode에서 실행
1. Xcode 상단 툴바에서 시뮬레이터 선택 (예: iPhone 15 Pro)
2. **Run** 버튼 클릭 (▶️) 또는 `⌘ + R`
3. 시뮬레이터가 자동으로 실행됨

#### 방법 2: 터미널에서 실행
```bash
npx cap run ios
```

#### 테스트 체크리스트
- [ ] 앱이 정상적으로 시작되는가?
- [ ] 모든 페이지가 정상적으로 표시되는가?
- [ ] 계산기 기능이 정상적으로 작동하는가?
- [ ] 구글 광고가 표시되지 않는가? ✓
- [ ] Safe Area가 올바르게 처리되는가? (노치/아일랜드 영역)

---

### 4. 실제 기기에서 테스트 (선택)

#### 준비물
- iPhone (iOS 13.0 이상)
- USB 케이블
- Apple Developer 계정 (무료 계정으로도 7일간 테스트 가능)

#### 실행 방법
1. iPhone을 Mac에 USB로 연결
2. iPhone에서 **신뢰** 프롬프트 승인
3. Xcode 상단에서 연결된 기기 선택
4. **Run** 버튼 클릭
5. iPhone에서 **설정 > 일반 > 기기 관리**로 이동
6. 개발자 앱 신뢰 설정

---

## 🚀 App Store 출시 준비

### 필수 준비물
- [ ] **Apple Developer 계정** ($99/년) - 필수
- [ ] **앱 아이콘** (1024x1024px)
- [ ] **스크린샷** 5장 이상
  - iPhone 6.7" (1290 x 2796px)
  - iPhone 6.5" (1242 x 2688px)
- [ ] **앱 설명 및 키워드**
- [ ] **개인정보 보호정책 URL**
  - 현재: https://aptgugu.com/privacy

### 출시 절차
자세한 내용은 `IOS_APP_STORE_RELEASE_GUIDE.md` 참고

간단 요약:
1. Xcode에서 Archive 생성
2. App Store Connect에 업로드
3. 앱 메타데이터 작성
4. 스크린샷 업로드
5. 심사 제출
6. 승인 대기 (보통 1~3일)

---

## 🔄 코드 수정 후 업데이트 방법

코드를 수정한 후에는 다음 명령어를 실행하세요:

```bash
# 1. Next.js 빌드
npm run build

# 2. iOS에 동기화
npx cap sync ios

# 3. Xcode에서 앱 재실행
# (Xcode가 열려있으면 자동으로 갱신됨)
```

---

## 📁 프로젝트 구조

```
Apartment-price-calculation/
├── src/                    # Next.js 소스 코드
│   └── app/               # 앱 페이지들
├── out/                   # 빌드된 정적 파일 (자동 생성)
├── ios/                   # iOS 네이티브 프로젝트
│   └── App/
│       ├── App/           # 앱 리소스 (아이콘, 설정 등)
│       └── App.xcodeproj  # Xcode 프로젝트 파일
├── capacitor.config.ts    # Capacitor 설정
├── next.config.js         # Next.js 설정 (정적 빌드)
└── package.json
```

---

## ⚠️ 주의사항

### 제거된 기능
- **API 라우트**: `/api/shorten`, `/api/sitemap-ping` 제거
  - 이유: 정적 빌드에서는 서버 사이드 기능 사용 불가
- **동적 라우트**: `/result/[slug]` 제거
  - 이유: URL 단축 기능은 서버가 필요함
- **구글 광고**: 모든 AdSense 코드 제거
  - 이유: 요청사항에 따라 iOS 앱에서 제외

### 웹 버전과의 차이
- iOS 앱은 정적 빌드 기반
- 서버 사이드 렌더링(SSR) 없음
- API 라우트 없음
- 모든 데이터는 클라이언트 사이드에서 처리

---

## 🛠️ 문제 해결

### 빌드 오류 발생 시
```bash
# 1. node_modules 재설치
rm -rf node_modules package-lock.json
npm install

# 2. Next.js 캐시 삭제
rm -rf .next out

# 3. 재빌드
npm run build
npx cap sync ios
```

### Xcode에서 실행 안 될 때
1. **Product > Clean Build Folder** (`⇧⌘K`)
2. **Product > Build** (`⌘B`)
3. 다시 실행

### 시뮬레이터가 느릴 때
- **Hardware > Erase All Content and Settings**로 시뮬레이터 초기화

---

## 📚 유용한 리소스

- [Capacitor 공식 문서](https://capacitorjs.com/docs)
- [Apple Developer 가이드](https://developer.apple.com/documentation/)
- [App Store Connect](https://appstoreconnect.apple.com/)
- [Xcode 다운로드](https://apps.apple.com/app/xcode/id497799835)

---

## ✨ 현재 상태

**Xcode가 열려 있습니다!** 이제 위의 단계를 따라 진행하세요.

1. ✅ Apple Developer 계정 로그인
2. ✅ 앱 아이콘 추가
3. ✅ 시뮬레이터에서 테스트
4. ✅ 실제 기기에서 테스트 (선택)
5. ✅ App Store 출시 준비

---

**질문이나 문제가 있으면 언제든 물어보세요!** 🚀
